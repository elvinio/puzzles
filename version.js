/* ============================================================================
   version.js — single source of truth for the app version shown in the
   avatar popup and used by sw.js for cache-busting. Bump this on deploy.
   ========================================================================== */
self.PZ_VERSION = 'pz-v10';
